
@{
    ViewBag.Title = "ClassCreate";
    Layout = "~/Views/Shared/_Layout.Edit.cshtml";
}

@model ShiNengShiHui.Web.Models.Administrator.Class.ClassCreateModel

@Html.AntiForgeryToken()
<div class="form-horizontal">

    <div class="form-group">
        <label class="col-xs-4 control-label text-right">班级入学编号：</label>
        <div class="col-xs-6">
            @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            <span class="help-inline">@Html.ValidationMessageFor(m => m.Name)</span>
        </div>
    </div>


    <div class="form-group">
        <label class="col-xs-4 control-label text-right">班级名：</label>
        <div class="col-xs-6">
            @Html.TextBoxFor(m => m.Display, new { @class = "form-control" })
            <span class="help-inline">@Html.ValidationMessageFor(m => m.Display)</span>
        </div>
    </div>

    <div class="form-group">
        <label class="col-xs-4 control-label text-right">创建时间：</label>
        <div class="col-xs-6">
            @Html.TextBoxFor(m => m.InTime, new { id = "datetimepicker" })
            <span class="help-inline">@Html.ValidationMessageFor(m => m.InTime)</span>
        </div>
    </div>

</div>
